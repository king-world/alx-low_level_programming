#include "main.h"

#include <stdio.h>

/**
 * _puts - Main function
 * @str: String
 *
 * Return: Always 0 (Success)
 */

void _puts(char *str)
{
	puts("\"Programming is like building a multilingual puzzle");
	return (0);
}
